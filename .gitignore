.env
.env.local
.env.*.local
node_modules
dist
.next
.turbo
coverage
*.log
*.tsbuildinfo
prisma/dev.db

# Local-only tunnel client — not for committing
packages/client/

# Publishing guide — local reference only
PUBLISHING.md

# Local task files — agent work notes, not committed
tasks/
